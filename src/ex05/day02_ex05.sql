SELECT name -- делаем отбор имен
FROM person -- из таблицы персоны 
WHERE gender = 'female' AND age > 25 -- отсеиваем по логической связке пола и возраста
ORDER BY name; -- сортируем по возрастанию
/*для поиска имен женщин старше 25 лет, результат сортуруется
по имени по возрастанию*/